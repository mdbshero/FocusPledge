name: Forbidden Terms Lint

on:
  push: {}
  pull_request: {}

jobs:
  forbidden_terms:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run forbidden terms check
        run: bash tools/check_forbidden_terms.sh
